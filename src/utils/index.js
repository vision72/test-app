// generally there would be more files in utils,
// for simplicity placed in index.js

// in this case, the ip address is specific to
// the local server you are running in your network
const IP_ADDRESS = '192.168.0.108';
export const API_URL = `http://${IP_ADDRESS}:1214/`;
